/** Telemetry charts rail (Task 8). Stub pending the full implementation. */

import type { RunData } from "./bsd1";
import type { Timeline } from "./timeline";

export function buildCharts(container: HTMLElement, run: RunData, timeline: Timeline): () => void {
  void run;
  void timeline;
  container.innerHTML = "";
  return () => {};
}
