/** Parameter sweep explorer (Task 9). Stub pending the full implementation. */

import type { Manifest } from "./player";

export function initSweep(
  container: HTMLElement,
  manifest: Manifest,
  onSelectRun: (runId: string) => void,
): void {
  void manifest;
  void onSelectRun;
  container.innerHTML = "";
}
